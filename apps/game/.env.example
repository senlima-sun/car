# Override the auth client's base URL if the API is cross-origin.
# Default (unset): `/api/auth`, served through the Vite dev proxy
# or the same origin in production.
# VITE_AUTH_BASE_URL=https://api.example.com/api/auth
