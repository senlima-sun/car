import type { PlacedObject } from '@/stores/useCustomizationStore'
import type { PitLaneData } from '@/stores/usePitStore'

export const DEFAULT_TRACK_NAME = 'Default Track'

// Placeholder - user will provide track JSON after using export
export const DEFAULT_TRACK_OBJECTS: PlacedObject[] = []

export const DEFAULT_TRACK_PIT_DATA: PitLaneData | null = null
